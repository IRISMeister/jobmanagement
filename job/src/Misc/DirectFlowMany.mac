ROUTINE DirectFlowMany
main() public {
	Set st=$ZH
	Set tSC = ##class(Ens.Director).CreateBusinessService("DirectFlowMany",.tService)
	If ($IsObject(tService)) {
		Set req=##class(Ens.Request).%New()
		Set tSC = tService.ProcessInput(req,.res)
		zw res
	}
	w "経過時間: "_($ZH-st)_",秒",!
}